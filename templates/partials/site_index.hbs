<ul>
  {{#each collections}}
    <li><strong>{{name}}</strong></li>
    <ul>
      {{#each pages}}
        <li><a href="{{../../relativeRoot}}{{path}}">{{title}}</a></li>
      {{/each}}
    </ul>
  {{/each}}
</ul>
